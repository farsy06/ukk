<section class="home-hero mb-4">
    <div class="row g-4 align-items-center">
        <div class="col-lg-7">
            <span class="home-kicker"><i class="fas fa-bolt me-2"></i>Sistem Peminjaman Digital</span>
            <h1 class="display-5 fw-bold mt-3 mb-3">Kelola Peminjaman Alat Tanpa Proses Rumit</h1>
            <p class="lead text-muted mb-4">
                eSarpra memudahkan pencatatan inventori, pengajuan pinjam, dan pelacakan status dalam satu alur kerja yang rapi.
            </p>

            <div class="d-flex flex-wrap gap-2">
                <% if (typeof user !== 'undefined' && user) { %>
                    <% if (user.role === 'admin') { %>
                        <a href="/admin" class="btn btn-primary btn-lg"><i class="fas fa-tachometer-alt me-2"></i>Dashboard Admin</a>
                    <% } else if (user.role === 'petugas') { %>
                        <a href="/petugas" class="btn btn-primary btn-lg"><i class="fas fa-tachometer-alt me-2"></i>Dashboard Petugas</a>
                    <% } else { %>
                        <a href="/alat" class="btn btn-primary btn-lg"><i class="fas fa-box-open me-2"></i>Lihat Alat</a>
                    <% } %>
                    <a href="/peminjaman" class="btn btn-outline-secondary btn-lg"><i class="fas fa-history me-2"></i>Riwayat</a>
                <% } else { %>
                    <a href="/login" class="btn btn-primary btn-lg"><i class="fas fa-right-to-bracket me-2"></i>Masuk</a>
                    <a href="/register" class="btn btn-outline-secondary btn-lg"><i class="fas fa-user-plus me-2"></i>Buat Akun</a>
                <% } %>
            </div>
        </div>

        <div class="col-lg-5">
            <div class="home-hero-panel">
                <h6 class="mb-3"><i class="fas fa-chart-line me-2"></i>Ringkasan Cepat</h6>
                <div class="row g-3">
                    <div class="col-6">
                        <div class="home-metric">
                            <div class="home-metric-label">Alat Tersedia</div>
                            <div class="home-metric-value" data-counter="<%= totalAlatTersedia || 0 %>">0</div>
                        </div>
                    </div>
                    <div class="col-6">
                        <div class="home-metric">
                            <div class="home-metric-label">Total Kategori</div>
                            <div class="home-metric-value" data-counter="<%= totalKategori || 0 %>">0</div>
                        </div>
                    </div>
                    <div class="col-6">
                        <div class="home-metric">
                            <div class="home-metric-label">Ditampilkan</div>
                            <div class="home-metric-value" data-counter="<%= alat ? alat.length : 0 %>">0</div>
                        </div>
                    </div>
                    <div class="col-6">
                        <div class="home-metric">
                            <div class="home-metric-label">Denda / Hari</div>
                            <div class="home-metric-value home-metric-small">Rp <%= overdueFinePerDayFormatted %></div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<section class="row g-4 mb-4">
    <div class="col-lg-8">
        <div class="card home-section-card">
            <div class="card-header d-flex justify-content-between align-items-center">
                <h5 class="mb-0"><i class="fas fa-boxes-stacked me-2"></i>Alat Tersedia Sekarang</h5>
                <a href="/alat" class="btn btn-sm btn-outline-secondary">Lihat Semua</a>
            </div>
            <div class="card-body">
                <% if (alat && alat.length > 0) { %>
                    <div class="row g-3">
                        <% alat.forEach((item) => { %>
                            <div class="col-md-6 col-xl-4">
                                <article class="home-alat-card h-100">
                                    <div class="home-alat-thumb">
                                        <% if (item.foto) { %>
                                            <img src="<%= item.foto %>" alt="Foto <%= item.nama_alat %>">
                                        <% } else { %>
                                            <div class="home-alat-placeholder">
                                                <i class="fas fa-toolbox"></i>
                                            </div>
                                        <% } %>
                                    </div>
                                    <div class="home-alat-body">
                                        <h6 class="mb-1"><%= item.nama_alat %></h6>
                                        <p class="small text-muted mb-2"><%= item.kategori ? item.kategori.nama_kategori : 'Tanpa kategori' %></p>
                                        <div class="d-flex justify-content-between align-items-center">
                                            <span class="badge bg-success">Stok <%= item.stok %></span>
                                            <% if (typeof user !== 'undefined' && user && user.role === 'peminjam') { %>
                                                <a href="/peminjaman/ajukan/<%= item.id %>" class="btn btn-sm btn-primary">Ajukan</a>
                                            <% } else { %>
                                                <a href="/alat" class="btn btn-sm btn-outline-secondary">Detail</a>
                                            <% } %>
                                        </div>
                                    </div>
                                </article>
                            </div>
                        <% }); %>
                    </div>
                <% } else { %>
                    <div class="text-center py-4">
                        <i class="fas fa-box-open fa-2x text-muted mb-2"></i>
                        <p class="text-muted mb-0">Belum ada alat tersedia saat ini.</p>
                    </div>
                <% } %>
            </div>
        </div>
    </div>

    <div class="col-lg-4">
        <div class="card home-section-card h-100">
            <div class="card-header">
                <h5 class="mb-0"><i class="fas fa-user-circle me-2"></i>Status Pengguna</h5>
            </div>
            <div class="card-body d-flex flex-column">
                <% if (typeof user !== 'undefined' && user) { %>
                    <div class="home-userbox mb-3">
                        <div class="home-avatar"><%= user.nama.charAt(0).toUpperCase() %></div>
                        <div>
                            <h6 class="mb-0"><%= user.nama %></h6>
                            <small class="text-muted"><%= user.email %></small>
                        </div>
                    </div>
                    <span class="badge bg-primary align-self-start mb-3"><%= user.role.toUpperCase() %></span>
                    <p class="text-muted small mb-3">Akses cepat sesuai peran Anda tersedia di bawah ini.</p>

                    <div class="d-grid gap-2 mt-auto">
                        <% if (user.role === 'admin') { %>
                            <a href="/admin" class="btn btn-primary"><i class="fas fa-sliders me-2"></i>Kelola Sistem</a>
                        <% } else if (user.role === 'petugas') { %>
                            <a href="/petugas" class="btn btn-primary"><i class="fas fa-clipboard-check me-2"></i>Validasi Peminjaman</a>
                        <% } else { %>
                            <a href="/alat" class="btn btn-primary"><i class="fas fa-box-open me-2"></i>Ajukan Peminjaman</a>
                            <a href="/peminjaman" class="btn btn-outline-secondary"><i class="fas fa-clock-rotate-left me-2"></i>Cek Riwayat</a>
                        <% } %>
                    </div>
                <% } else { %>
                    <div class="text-center my-auto">
                        <div class="home-avatar mx-auto mb-3">G</div>
                        <h6>Pengunjung</h6>
                        <p class="text-muted small mb-3">Masuk untuk mulai mengajukan peminjaman alat.</p>
                        <div class="d-grid gap-2">
                            <a href="/login" class="btn btn-primary"><i class="fas fa-right-to-bracket me-2"></i>Login</a>
                            <a href="/register" class="btn btn-outline-secondary"><i class="fas fa-user-plus me-2"></i>Register</a>
                        </div>
                    </div>
                <% } %>
            </div>
        </div>
    </div>
</section>

<section class="card home-section-card mb-4">
    <div class="card-header">
        <h5 class="mb-0"><i class="fas fa-circle-info me-2"></i>Kebijakan & Panduan Singkat</h5>
    </div>
    <div class="card-body">
        <div class="row g-3">
            <div class="col-md-4">
                <div class="home-info-card">
                    <h6><i class="fas fa-calendar-days me-2 text-primary"></i>Batas Peminjaman</h6>
                    <p class="small text-muted mb-0">Durasi maksimal peminjaman 7 hari. Keterlambatan dikenakan denda Rp <%= overdueFinePerDayFormatted %>/hari.</p>
                </div>
            </div>
            <div class="col-md-4">
                <div class="home-info-card">
                    <h6><i class="fas fa-shield-halved me-2 text-success"></i>Keamanan Data</h6>
                    <p class="small text-muted mb-0">Data akun dan aktivitas peminjaman dilindungi, termasuk validasi sesi dan proteksi CSRF.</p>
                </div>
            </div>
            <div class="col-md-4">
                <div class="home-info-card">
                    <h6><i class="fas fa-headset me-2 text-info"></i>Bantuan</h6>
                    <p class="small text-muted mb-0">Jika ada kendala pada proses peminjaman/pengembalian, hubungi petugas atau admin melalui kanal resmi.</p>
                </div>
            </div>
        </div>
    </div>
</section>

<script src="/js/home.js"></script>
