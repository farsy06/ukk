<div class="row g-4 align-items-stretch auth-layout">
    <div class="col-lg-5">
        <div class="card border-0 shadow-sm h-100 overflow-hidden auth-aside-card auth-aside-card-login">
            <div class="card-body p-4 p-md-5 d-flex flex-column justify-content-between auth-aside-content">
                <div>
                    <div class="small text-uppercase fw-semibold opacity-75 mb-3">Pemulihan Akun</div>
                    <h2 class="fw-bold mb-3">Lupa password akun Anda?</h2>
                    <p class="mb-4 opacity-75">
                        Masukkan username atau email akun Anda untuk membuat tautan reset password.
                    </p>
                </div>
                <ul class="list-unstyled mb-0">
                    <li class="mb-2"><i class="fas fa-shield-halved me-2"></i>Tautan reset bersifat sementara</li>
                    <li class="mb-2"><i class="fas fa-link me-2"></i>Setiap permintaan membuat token baru</li>
                    <li><i class="fas fa-user-lock me-2"></i>Informasi akun tetap disamarkan</li>
                </ul>
            </div>
        </div>
    </div>

    <div class="col-lg-7">
        <div class="card border-0 shadow h-100 auth-form-card">
            <div class="card-header border-0 pt-4 px-4 px-md-5">
                <h4 class="mb-1"><i class="fas fa-key me-2 text-warning"></i>Lupa Password</h4>
                <p class="text-muted mb-0">Isi data akun untuk memulai reset password.</p>
            </div>

            <div class="card-body p-4 p-md-5">
                <form action="/forgot-password" method="POST" id="forgotPasswordForm" novalidate>
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">

                    <div class="mb-3">
                        <label for="identifier" class="form-label">Username atau Email <span class="text-danger">*</span></label>
                        <div class="input-group">
                            <span class="input-group-text"><i class="fas fa-user"></i></span>
                            <input
                                type="text"
                                class="form-control"
                                id="identifier"
                                name="identifier"
                                value="<%= typeof data !== 'undefined' && data ? data.identifier : '' %>"
                                autocomplete="username"
                                required
                            >
                        </div>
                    </div>

                    <div class="d-grid">
                        <button type="submit" class="btn btn-warning btn-lg" id="forgotPasswordSubmitButton">
                            <i class="fas fa-paper-plane me-2"></i>Kirim Tautan Reset
                        </button>
                    </div>
                </form>
            </div>

            <div class="card-footer border-0 pb-4 px-4 px-md-5">
                <div class="text-center">
                    <span class="text-muted">Kembali ke halaman login?</span>
                    <a href="/login" class="text-decoration-none fw-semibold ms-1">Masuk di sini</a>
                </div>
            </div>
        </div>
    </div>
</div>

<script src="/js/auth-common.js"></script>
<script src="/js/auth-forgot.js"></script>
