<div class="row g-4 align-items-stretch auth-layout">
    <div class="col-lg-5">
        <div class="card border-0 shadow-sm h-100 overflow-hidden auth-aside-card auth-aside-card-register">
            <div class="card-body p-4 p-md-5 d-flex flex-column justify-content-between auth-aside-content">
                <div>
                    <div class="small text-uppercase fw-semibold opacity-75 mb-3">Atur Password Baru</div>
                    <h2 class="fw-bold mb-3">Buat password baru yang aman</h2>
                    <p class="mb-4 opacity-75">
                        Password baru akan langsung menggantikan password lama dan sesi remember me akan dinonaktifkan.
                    </p>
                </div>
                <ul class="list-unstyled mb-0">
                    <li class="mb-2"><i class="fas fa-lock me-2"></i>Minimal 8 karakter</li>
                    <li class="mb-2"><i class="fas fa-font me-2"></i>Gunakan huruf besar, kecil, angka, dan simbol</li>
                    <li><i class="fas fa-shield-halved me-2"></i>Token reset hanya berlaku sementara</li>
                </ul>
            </div>
        </div>
    </div>

    <div class="col-lg-7">
        <div class="card border-0 shadow h-100 auth-form-card">
            <div class="card-header border-0 pt-4 px-4 px-md-5">
                <h4 class="mb-1"><i class="fas fa-unlock-keyhole me-2 text-success"></i>Reset Password</h4>
                <p class="text-muted mb-0">Masukkan password baru untuk akun Anda.</p>
            </div>

            <div class="card-body p-4 p-md-5">
                <form action="/reset-password/<%= token %>" method="POST" id="resetPasswordForm" novalidate>
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">

                    <div class="row g-3">
                        <div class="col-md-6">
                            <label for="password" class="form-label">Password Baru <span class="text-danger">*</span></label>
                            <div class="input-group">
                                <span class="input-group-text"><i class="fas fa-lock"></i></span>
                                <input
                                    type="password"
                                    class="form-control"
                                    id="password"
                                    name="password"
                                    autocomplete="new-password"
                                    required
                                >
                                <button class="btn btn-outline-secondary" type="button" id="togglePassword" aria-label="Tampilkan password baru">
                                    <i class="fas fa-eye"></i>
                                </button>
                            </div>
                        </div>

                        <div class="col-md-6">
                            <label for="confirmPassword" class="form-label">Konfirmasi Password <span class="text-danger">*</span></label>
                            <div class="input-group">
                                <span class="input-group-text"><i class="fas fa-lock"></i></span>
                                <input
                                    type="password"
                                    class="form-control"
                                    id="confirmPassword"
                                    name="confirmPassword"
                                    autocomplete="new-password"
                                    required
                                >
                                <button class="btn btn-outline-secondary" type="button" id="toggleConfirmPassword" aria-label="Tampilkan konfirmasi password">
                                    <i class="fas fa-eye"></i>
                                </button>
                            </div>
                            <div id="password-match-message" class="form-text"></div>
                        </div>
                    </div>

                    <div class="mt-3 p-3 rounded border auth-password-box">
                        <div class="small fw-semibold mb-2">Kriteria password:</div>
                        <ul class="mb-0 small ps-3">
                            <li id="length-check" class="text-danger"><i class="fas fa-times me-1"></i>Minimal 8 karakter</li>
                            <li id="uppercase-check" class="text-danger"><i class="fas fa-times me-1"></i>Mengandung huruf besar (A-Z)</li>
                            <li id="lowercase-check" class="text-danger"><i class="fas fa-times me-1"></i>Mengandung huruf kecil (a-z)</li>
                            <li id="number-check" class="text-danger"><i class="fas fa-times me-1"></i>Mengandung angka (0-9)</li>
                            <li id="special-check" class="text-danger"><i class="fas fa-times me-1"></i>Mengandung karakter spesial (!@#$%^&*)</li>
                        </ul>
                    </div>

                    <div class="d-grid mt-4">
                        <button type="submit" class="btn btn-success btn-lg" id="resetPasswordSubmitButton">
                            <i class="fas fa-check me-2"></i>Simpan Password Baru
                        </button>
                    </div>
                </form>
            </div>

            <div class="card-footer border-0 pb-4 px-4 px-md-5">
                <div class="text-center">
                    <span class="text-muted">Kembali ke login?</span>
                    <a href="/login" class="text-decoration-none fw-semibold ms-1">Masuk di sini</a>
                </div>
            </div>
        </div>
    </div>
</div>

<script src="/js/auth-common.js"></script>
<script src="/js/auth-reset.js"></script>
