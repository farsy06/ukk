<div class="row g-4 align-items-stretch auth-layout">
    <div class="col-lg-5">
        <div class="card border-0 shadow-sm h-100 overflow-hidden auth-aside-card auth-aside-card-login">
            <div class="card-body p-4 p-md-5 d-flex flex-column justify-content-between auth-aside-content">
                <div>
                    <div class="small text-uppercase fw-semibold opacity-75 mb-3">Portal eSarpra</div>
                    <h2 class="fw-bold mb-3">Masuk untuk kelola peminjaman alat</h2>
                    <p class="mb-4 opacity-75">
                        Pantau status alat, ajukan peminjaman, dan cek riwayat transaksi dalam satu dashboard.
                    </p>
                </div>
                <ul class="list-unstyled mb-0">
                    <li class="mb-2"><i class="fas fa-circle-check me-2"></i> Proses cepat dan terstruktur</li>
                    <li class="mb-2"><i class="fas fa-circle-check me-2"></i> Keamanan berbasis sesi dan CSRF</li>
                    <li><i class="fas fa-circle-check me-2"></i> Riwayat aktivitas tersimpan otomatis</li>
                </ul>
            </div>
        </div>
    </div>

    <div class="col-lg-7">
        <div class="card border-0 shadow h-100 auth-form-card">
            <div class="card-header border-0 pt-4 px-4 px-md-5">
                <h4 class="mb-1"><i class="fas fa-sign-in-alt me-2 text-primary"></i>Login</h4>
                <p class="text-muted mb-0">Gunakan akun Anda untuk melanjutkan.</p>
            </div>

            <div class="card-body p-4 p-md-5">
                <form action="/login" method="POST" id="loginForm" novalidate>
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">

                    <div class="mb-3">
                        <label for="username" class="form-label">Username <span class="text-danger">*</span></label>
                        <div class="input-group">
                            <span class="input-group-text"><i class="fas fa-user"></i></span>
                            <input
                                type="text"
                                class="form-control"
                                id="username"
                                name="username"
                                autocomplete="username"
                                required
                            >
                        </div>
                    </div>

                    <div class="mb-3">
                        <label for="password" class="form-label">Password <span class="text-danger">*</span></label>
                        <div class="input-group">
                            <span class="input-group-text"><i class="fas fa-lock"></i></span>
                            <input
                                type="password"
                                class="form-control"
                                id="password"
                                name="password"
                                autocomplete="current-password"
                                required
                            >
                            <button class="btn btn-outline-secondary" type="button" id="togglePassword" aria-label="Tampilkan password">
                                <i class="fas fa-eye"></i>
                            </button>
                        </div>
                    </div>

                    <div class="d-flex justify-content-between align-items-center mb-4">
                        <div class="form-check mb-0">
                            <input class="form-check-input" type="checkbox" id="rememberMe" name="rememberMe">
                            <label class="form-check-label" for="rememberMe">Ingat saya</label>
                        </div>
                        <div class="text-end">
                            <a href="/forgot-password" class="small text-decoration-none d-block">Lupa password?</a>
                            <a href="/tos" class="small text-decoration-none">Syarat & ketentuan</a>
                        </div>
                    </div>

                    <div class="d-grid">
                        <button type="submit" class="btn btn-primary btn-lg" id="loginSubmitButton">
                            <i class="fas fa-right-to-bracket me-2"></i>Masuk
                        </button>
                    </div>
                </form>
            </div>

            <div class="card-footer border-0 pb-4 px-4 px-md-5">
                <div class="text-center">
                    <span class="text-muted">Belum punya akun?</span>
                    <a href="/register" class="text-decoration-none fw-semibold ms-1">Daftar sekarang</a>
                </div>
            </div>
        </div>
    </div>
</div>

<script src="/js/auth-common.js"></script>
<script src="/js/auth-login.js"></script>
